{
  "name": "namas-server",
  "version": "1.0.0",
  "description": "Backend server for NAMAS Architecture website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx nodemon --config nodemon.json server.js",
    "dev:verbose": "npx nodemon --config nodemon.json --verbose server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": ["express", "api", "architecture", "namas"],
  "author": "NAMAS Architecture",
  "license": "MIT"
}
