# Backend environment example
NODE_ENV=production
PORT=8080
HOST=0.0.0.0

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=https://www.namasbhutan.com,https://namasbhutan.com,http://localhost:3000

# Optional: customize data and uploads directories
# DATA_DIR=/absolute/path/to/data
# UPLOADS_DIR=/absolute/path/to/uploads
# BACKUP_DIR=/absolute/path/to/backups

# Email (recommend using env vars; do not commit real creds)
EMAIL_USER=namasdesign2021@gmail.com
EMAIL_PASS=ojsy ayyj igap htko